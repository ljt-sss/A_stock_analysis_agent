# API

统一前缀 `/api/v1`。核心资源：

- `/stocks`：搜索、驾驶舱、估值、财务、分部和同行。
- `/watchlists`：分组、股票和投资假设。
- `/reports`：财报列表、更新、解析、chunk 和流水线。
- `/agent/tasks`：创建任务、状态、步骤、工具日志和 SSE。
- `/wiki`、`/memory`：知识库与长期记忆。
- `/skills`、`/mcp/tools`：技能和工具调用。
- `/evals`：指标、用例、运行和结果。

完整 OpenAPI 请访问 `/docs`。

